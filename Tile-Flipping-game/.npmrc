# Enforce pnpm — disallow npm/yarn from being used on this project
engine-strict=false

# Hoist nothing by default (pnpm strict isolation)
shamefully-hoist=false

# Peer dependency auto-install
auto-install-peers=true
